

let initialState = {
    navLinks: ['home', 'about', 'skills', 'experience', 'education', 'portfolio', 'contact', 'blog']
};

export const navReducer = (state = initialState, action: any) => {
    switch (action.type) {
        default: return state;
    }
};